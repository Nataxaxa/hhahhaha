export default function Main({ children }) {
  return (
    <div
      style={{
        backgroundColor: "FFFFFF",
        textAlign: "center",
        fontSize: 100,
        height: "100%",
      }}
    >
      {children}
    </div>
  );
}
